/**
 * P2026 modules — side-effect imports for all active modules.
 *
 * Add a new module by creating src/modules/{name}/index.js and importing
 * it here. Each module is responsible for its own initialization.
 */
import './mentions';
